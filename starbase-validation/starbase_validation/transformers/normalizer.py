"""Standardize formats and normalize values before load."""


def normalize(records):
    """Normalize and standardize record fields (e.g. taxonomy strings, dates, nulls). Returns normalized records."""
    # TODO: Consistent nulls, trimming, case, date formats.
    return records

